var totalNumbers = document.querySelectorAll(".drum").length;

for ( var i = 0 ; i<totalNumbers; i++) {

document.querySelectorAll(".drum")[i].addEventListener("click", clickable);

function clickable() {

    switch (key) {
        case value:
            
            break;
    
        default:
            break;
    }

    }
};















// var audio = new Audio ('sounds/snare.mp3');
// audio.play();